type TIngredient = {
  foodId: string;
  quantity: number;
  measure: string | null; // Measure object lacks documentation
  weight: number;
  food: string; // Food object lacks documentation
  text: string;
};
